# Security

Why executes the exact argument vector supplied by the user and never implicitly invokes a shell. It does not capture syscall buffer contents or network payloads. The current bind decoder reads only the bounded socket-address structure needed for diagnosis.

Why must not be installed setuid. Linux ptrace and procfs security policy may reduce available evidence; this is handled as missing enrichment or an explicit tracing error, never by escalating privileges automatically.
